pipeline {
    agent any

    stages {
        stage ('Docker')
        {
            steps{
                sh 'docker build -f CI/Dockerfile-playwright -t my-playwright .'
                sh 'docker build -f CI/Dockerfile-amazon-cli -t my-aws-cli .'
            }
        }
    }
}